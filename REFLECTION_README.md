# Universal Self-Reflection System Integration

This directory contains the Universal Self-Reflection System integrated into your AI agent.

## 🚀 Quick Start

### 1. Install Dependencies

```bash
pip install -r reflection_requirements.txt
```

### 2. Configure Your Agent

Edit `reflection_integration.py` and implement the required methods:

- `get_metadata()` - Return agent metadata
- `get_performance_metrics()` - Return performance metrics
- `get_assets()` - Return list of assets
- `apply_optimization()` - Apply optimization suggestions

### 3. Customize Configuration

Edit `reflection_config.yaml` to customize:

- Which modules to enable
- Naming conventions
- Documentation preferences
- Evolution parameters

### 4. Run Daily Reflection

```bash
python run_reflection.py
```

Or integrate into your agent's main loop:

```python
from reflection_integration import setup_reflection_system

# In your agent initialization
reflection_engine = setup_reflection_system(your_agent)

# Run daily (e.g., via cron or scheduler)
result = reflection_engine.run_daily_reflection()
```

## 📦 Directory Structure

```
your-agent/
├── reflection/              # Self-reflection system core
│   ├── core/               # Core engine and interfaces
│   └── modules/            # Reflection modules
├── data/
│   ├── reflection/         # Generated reports
│   └── learning/           # Learning history
├── reflection_integration.py   # Your agent integration
├── reflection_config.yaml      # Configuration
└── run_reflection.py          # Quick start script
```

## 📚 Modules

- **Inventory**: Tracks all assets and dependencies
- **Taxonomy**: Enforces naming conventions and classifications
- **Documentation**: Auto-generates README, changelog, API docs
- **Evolution**: Analyzes performance and generates optimizations
- **Reporting**: Creates comprehensive reports in multiple formats

## 🔧 Advanced Usage

### Schedule Daily Reflection

Add to crontab:

```bash
0 0 * * * cd /path/to/agent && python run_reflection.py
```

### Enable Auto-Optimization

In `reflection_config.yaml`:

```yaml
evolution:
  auto_optimize: true  # Automatically apply safe optimizations
```

### Custom Modules

Create custom reflection modules in `reflection/modules/custom/`

## 📊 Viewing Results

Reports are saved to `data/reflection/`:

- `reflection_YYYYMMDD_HHMMSS.json` - Full JSON report
- `reflection_YYYYMMDD_HHMMSS.md` - Markdown summary

## 🆘 Support

For issues or questions, refer to the main Universal Self-Reflection System documentation.

---

*Auto-generated by Universal Self-Reflection System v1.0.0*
